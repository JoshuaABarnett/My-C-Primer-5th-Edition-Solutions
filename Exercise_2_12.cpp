// Which , if any, of the following names are invalid?
// (a) int double = 3.14;
// (b) int _;
// (c) int catch-22;
// (d) int 1_or_2 = 1;
// (e) double Double = 3.14;

// (a) invalid, double is a keyword that cannot be used as an identifier
// (b) invalid if defined outside a function, valid if defined within a function
// (c) invalid, there is a dash '-', identifiers can be composed of letters, digits, and the underscore character
// (d) valid
// (e) valid, Double is different than the keyword double